> **Status:** Historical design prompt. Retained for design history; use
> [`../docs/architecture.md`](../docs/architecture.md) for current behavior.

We were able to successfully make V0.  It is working pretty fine. It is using openai agents SDK for the single agent that is working. Now we want to replace it with our custom agent built using lang-graph and completely remove openai agents sdk code and extra filler code (if any). Rest everything will be unchanged on a higher level.

Keep in mind that the behaviour of the project should not change, we are just building our own custom agent. This should not change project behaviour. I need you to design the lang-graph agent, the nodes, the edges, conditionals and tools that would be used to replace the openai agents sdk
